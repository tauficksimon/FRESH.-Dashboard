[build]
  # No build command needed - index.html is pre-generated
  command = ""
  publish = "."

# Ignore Python files - only deploy HTML/JS/CSS
[build.processing]
  skip_processing = false

# Don't install Python dependencies
[build.environment]
  # Disable automatic Python detection
  PYTHON_VERSION = ""
